export default interface BuildOptionsInterface {
  enable_attachment_pages: boolean;
  enable_url_finder: boolean;
  enable_feed: boolean;

  enable_rss_feed_atom: boolean;
  enable_rss_feed_rdf: boolean;
  enable_rss_feed_rss: boolean;

  enable_rss_feed_taxonomy: boolean;
  enable_rss_feed_author: boolean;
  enable_rss_feed_post_comment: boolean;

  enable_author_pages: boolean;
  enable_dates_pages: boolean;
  enable_terms_pages: boolean;

  enable_shortlinks: boolean;
}
